#ifndef MATRIZ_REF_H
#define MATRIZ_REF_H

template<typename T, size_t N>
class matriz_ref
{
  public:
    matriz_ref(const matriz);
    virtual ~matriz_ref();

  protected:

  private:
};

#endif // MATRIZ_REF_H
